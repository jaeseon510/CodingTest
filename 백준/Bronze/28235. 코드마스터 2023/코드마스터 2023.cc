#include<iostream>
using namespace std;
int main() {
	char s;
	cin >> s;
	if (s == 'S') cout << "HIGHSCHOOL";
	if (s == 'C') cout << "MASTER";
	if (s == '2') cout << "0611";
	if (s == 'A') cout << "CONTEST";
}